import 'package:flutter/material.dart';

const kPrimaryColor =
    Colors.cyan; // Main color used for branding, buttons, etc.
const kSecondaryColor =
    Color(0xFF575757); // Secondary, used to complement the primary color
const kAccentColor = Colors.white; // Accent, used for contrasting elements
var kBackgroundColor = Colors.grey.shade100; // Background color for screens
const kErrorColor = Colors.red; // Error states or messages
const kDisabledColor = Color.fromARGB(255, 87, 87, 87);
const kFontColor = Colors.black;
var kHintColor = Colors.grey.shade800;
